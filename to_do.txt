[v] create a local Docker / PostgreSQL / Prisma / Node / TS backend
[v] connect local frontend to local backend while they're running from my PC
[v] push both to gh
[v] ui - https://www.gameuidatabase.com/uploads/Bridge-Constructor-The-Walking-Dead07242021-103945-71130.jpg
[v] connect ui components with a story object
[v] read, understand the logic of and fix portrait ordering
[v] fullscreen height and width
[v] responsive on mobile
[v] no jittery switches
[v] decide on a data structure to use in both FE and BE
- Player class that holds an empty array of booleans and every stat as public field
- Script inits a this.Player instance in constructor
- Lines are an array of nodes: line, choice, split.
Script walks forward through nodes and returns a chain of sequential lines until it hits a choice or split node.
o choice node:
    - Each choice option updates player stats.
    - Script auto-creates a boolean per choice (e.g. chose_X = true).
    - Each choice leads to a new chain start index.
o split node:
    - Checks player booleans/stats.
    - Picks the correct next index.
    - Ends the current chain.
    - FE sends user choices to BE; BE handles all state updates and next-node logic.
    - FE never predicts story flow; BE computes next chain every time.
[v] create the data structure in the backend

[v] for now, mock the login by a single button on frontend + just one user on backend

// frontend:
// - shows line nodes, line by line, as the player clicks
// - shows choices
// - sends which choice was made

// backend:
// - sends line nodes holds the "player" object
// - updates the player object on the DB based on choice made
// - sends choices
// - sends the next line node based on choice made or split node condition

[v] write a master document with types that both will share, namely lines and nodes and Character (minus lastSpokeIndex)
[_] finish backend and frontend simultaneously:
[_] player is a psql table
[_] remove the temporary data_structures file and instead write contained middleware without side effects
[_] lastSpokeIndex is a frontend function
[_] Script class is not a thing anywhere
[_] remove the temporary vn_objects and vn_script files
[_] logging in / signing up

[_] how to assign portraits between frontend and backend?
[_] how to assign background changes between frontend and backend?
[_] create views on backend to easily write story objects 
[_] init entering player name